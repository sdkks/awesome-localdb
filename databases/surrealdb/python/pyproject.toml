[project]
name = "awesome-localdb-surrealdb-python"
version = "0.1.0"
description = "Recipe examples for SurrealDB"
requires-python = ">=3.9"
dependencies = [
    "surrealdb>=2.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
