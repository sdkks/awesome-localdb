[project]
name = "awesome-localdb-arcadedb-python"
version = "0.1.0"
description = "Recipe examples for ArcadeDB"
requires-python = ">=3.10"
dependencies = [
    "arcadedb-embedded",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
