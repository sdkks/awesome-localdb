[project]
name = "awesome-localdb-tinydb-python"
version = "0.1.0"
description = "Recipe examples for TinyDB"
requires-python = ">=3.9"
dependencies = [
    "tinydb",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
