[project]
name = "awesome-localdb-chdb-python"
version = "0.1.0"
description = "Recipe examples for chDB"
requires-python = ">=3.9"
dependencies = [
    "chdb>=3.0.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
