[project]
name = "awesome-localdb-sqlite-python"
version = "0.1.0"
description = "Recipe examples for SQLite"
requires-python = ">=3.9"
dependencies = []

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
