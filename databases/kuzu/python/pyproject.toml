[project]
name = "awesome-localdb-kuzu-python"
version = "0.1.0"
description = "Recipe examples for Kuzu"
requires-python = ">=3.9"
dependencies = [
    "kuzu>=0.11.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
