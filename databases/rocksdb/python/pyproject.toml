[project]
name = "awesome-localdb-rocksdb-python"
version = "0.1.0"
description = "Recipe examples for RocksDB"
requires-python = ">=3.9"
dependencies = [
    "rocksdb-py",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
