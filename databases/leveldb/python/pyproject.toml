[project]
name = "awesome-localdb-leveldb-python"
version = "0.1.0"
description = "Recipe examples for LevelDB"
requires-python = ">=3.9"
dependencies = [
    "plyvel",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
