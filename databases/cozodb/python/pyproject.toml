[project]
name = "awesome-localdb-cozodb-python"
version = "0.1.0"
description = "Recipe examples for CozoDB"
requires-python = ">=3.9"
dependencies = [
    "pycozo[embedded]>=0.7.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
