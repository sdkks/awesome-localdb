[project]
name = "awesome-localdb-chromadb-python"
version = "0.1.0"
description = "Recipe examples for ChromaDB"
requires-python = ">=3.9"
dependencies = [
    "chromadb",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
