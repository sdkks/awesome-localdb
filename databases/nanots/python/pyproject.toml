[project]
name = "awesome-localdb-nanots-python"
version = "0.1.0"
description = "Recipe examples for NanoTS"
requires-python = ">=3.9"
dependencies = [
    "nanots>=0.1.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
